<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="stream_accept_del_auto">
 <title>Одобрение потоков данных в автоматическом режиме</title>
 <body>
 <p>В интерфейсе <term keyref="appliance"/>а верхнего уровня есть возможность создания правил для
      автоматического присвоения статуса потокам данных, либо для автоматического удаления. Правила
      применяются к потокам, попадающим под параметры потоков, указанные в правиле.</p>
 <p>Для настройки правил доступны следующие параметры:</p>
 <ol id="ol_aqn_gjv_pnb">
 <li><uicontrol>Номер — целое число, определяющее приоритет (порядок применения) правил: чем меньше
          число, </uicontrol>тем выше приоритет. Правила при создании автоматически нумеруются с
        интервалом в 100 единиц. Возможно изменять номера правила вручную на любое другое число,
        отличное от уже номеров, уже присвоенных другим правилам.</li>
 <li><uicontrol>Наименование правила</uicontrol> (должно быть уникальным).</li>
 <li><uicontrol>Комментарий к правилу</uicontrol>. Указывается администратором в качестве причины
        установления статуса потокам выбранным правилом. Указанный комментарий отображается на
        странице «Потоки» для тех потоков, статус которых был изменен в результате применения
        правила.</li>
 <li><uicontrol>Фильтр</uicontrol> — один или несколько параметров потоков, к которым необходимо
        применить выбранное правило. Список фильтров для настройки правила совпадает со списком
        фильтров на странице <cite keyref="network_inspection_fp"/> → <cite keyref="dataflow_fp"/>
        (описание фильтров приведено в разделе «<xref href="../stream_list_filter.dita"/>»).</li>
 <li><uicontrol>Действие</uicontrol>, которое необходимо выполнить с потоками (одобрить или временно
        одобрить).</li>
 <li>Чекбокс <uicontrol>Активность правила</uicontrol>.</li>
 </ol>
 <p>Параметры правил потоков данных, недоступные для редактирования:</p>
 <ul id="ol_bqn_gjv_pnb">
 <li>время создания правила;</li>
 <li>время изменения правила;</li>
 <li>автор правила — последний пользователь, изменивший правило. Если правило настроено для
        автоматического одобрения потоков, его автор будет отображаться на странице <cite
          keyref="dataflow_fp"/> в качестве пользователя, который выполнил одобрение потоков с
        помощью этого правила.</li>
 </ul>
    <note id="note_u15_dk4_kyb">
      <p>Особенности работы правил :</p>
      <ul id="ul_cqn_gjv_pnb">
        <li>Управлять потоками данных с использованием правил можно только для новых потоков,
          которые были обнаружены после создания правил, под которые они попадают.</li>
        <li>Правила, которые необходимо применять к новым потокам данных, не должны содержать
          фильтры <uicontrol>Дата и время обнаружения</uicontrol>, <uicontrol>Дата и время
            одобрения</uicontrol>,<uicontrol> Комментарий</uicontrol>, <uicontrol>Только
            одобренные/неодобренные</uicontrol>.</li>
        <li>Один поток обрабатывается первым по приоритету подходящим правилом. Оставшиеся
          подходящие правила с меньшим приоритетом к тому же потоку не будут применяться.</li>
        <li>Если в фильтре правила настроен параметр <uicontrol>Протокол</uicontrol>, указанный
          фильтр будет применяться только к последнему распознанному в потоке протоколу.</li>
        <li>Правила временного одобрения потоков в автоматическом режиме перестают действовать по
          истечении указанных даты и времени окончания одобрения. Если поток был обнаружен после
          окончания действия правила временного одобрения, то статус этого потока не изменится.</li>
      </ul>
    </note>
 </body>
</topic>
